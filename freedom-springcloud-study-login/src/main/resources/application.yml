server:
  port: 8090

spring:
  devtools:
    restart:
      enabled: true
      additional-paths:
      - src/main/java
      - src/main/resources
  application:
    name: login
      
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8091/eureka